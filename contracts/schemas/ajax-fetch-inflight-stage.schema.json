{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ajaxFetchInflightStage",
  "description": "Diagnostic stage tracking AJAX request from view_updater_build_advance.js to ViewUpdater.php",
  "type": "object",
  "required": ["nonce"],
  "additionalProperties": false,
  "properties": {
    "nonce": { "type": "string", "minLength": 1 },
    "requestId": { "type": "string", "pattern": "^[a-zA-Z0-9]{8,64}$" }
  }
}
