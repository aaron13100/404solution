{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ajaxAdminEndpointSupportEnvelope",
  "description": "Shared admin AJAX response envelope emitted by Ajax_AdminEndpointSupport (success flag plus optional data payload).",
  "type": "object",
  "required": ["success"],
  "properties": {
    "success": { "type": "boolean" },
    "data": {}
  }
}
