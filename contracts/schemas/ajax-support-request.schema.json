{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "abj404_support_request",
  "description": "Support request FormData AJAX from SupportRequest.js to Ajax_SupportRequest.php",
  "type": "object",
  "required": ["nonce", "triggered_from"],
  "additionalProperties": false,
  "properties": {
    "nonce": { "type": "string", "minLength": 1 },
    "triggered_from": {
      "type": "string",
      "enum": ["redirects_page", "captured_404s_page", "plugins_row_action", "settings_debug", "system_corrupt_install"]
    },
    "user_message": { "type": "string", "maxLength": 2000 },
    "reply_email": { "type": "string", "maxLength": 254 },
    "client_telemetry": { "type": "string", "maxLength": 32768, "description": "Drained client transport-attempt buffer (JSON array of per-attempt browser timeline records). Appended to debug_log_excerpt server-side." },
    "sessionId": { "type": "string", "maxLength": 64, "description": "Immutable request ledger: the browser tab/session this report is sent from. Scopes the detach A/B verdict computed into debug_log_excerpt server-side." }
  }
}
